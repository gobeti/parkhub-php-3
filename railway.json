{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "preDeployCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate --force --no-interaction || true",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
  }
}
